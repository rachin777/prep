#pragma once

// Method to check unique characters in given string
bool string_has_unique_chars(char *);

// Method to reverse a given string in place
char * reverse_string(char *);

// Method to find if the given strings are permutation of each other
bool strings_are_permutation(char *str1, char *str2);